# 教学引擎 MVP 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把产品从「相机识物工具」换成「引导式英语学习对话」——学习者说自己的生活，系统用四种方法按四拍把他逐层教会，全程引导而不代替决定。

**Architecture:** 全部教学逻辑放 `web/units/` 下的**纯函数模块**（零浏览器 API、零副作用，可在 Node 直接测）；模型调用被夹在「组装提示词 → 调模型 → 校验输出」之间，**校验不通过就重生成、再不过就降级为模板话术**。知识资产是数据文件 + 格式校验器（内容工程，不是代码）。旧形态的 `web/app.mjs` 与 `web/units/recognize|frame-qc|camera|album|pick-word|scheduler` **保留不动**（`DEC-…db.56` 的 `FUTURE_SCOPE`：识物降级不删除），新形态落在新的 `web/units/teach/**` 与 `web/dialogue.mjs`，两者互不 import。

**Tech Stack:** 原生 ES module + 手写 DOM（无框架）、`node --test`、Playwright（浏览器核验用）、DeepSeek API（前端直连、自带 Key）。

## Global Constraints

- 测试命令一律用**裸 `node --test`**（`node --test tests/` 在 Windows 上必红）。开发探针放 `tmp/probes/`，**文件名不许含 `test`**（裸命令会递归扫 `tmp/`）。
- 源文件用 **LF**，不许带行尾空白。
- **零外部资源**：不许外部字体/脚本/样式；样式单源 `web/styles.css`，不许内联 `<style>`；组件规则不许写死颜色（走令牌）。
- 纯逻辑模块的既有约定（照抄 `web/units/state-machine.mjs`）：**非法动作静默忽略并返回 `false`**（不抛错），**契约被违反（参数类型不对）响亮抛错 `TypeError`**；枚举用 `Object.freeze`。
- 每个模块文件头必须写清**为什么这么设计**与**代价**（本仓库的既定风格；后人靠它理解约束，不是靠读实现）。
- 不新增事件类型（`DEC-…b3.56` 的红线 3 族）；本轮**不落任何新事件**，先只做会话内逻辑。
- **PROPOSE 强度**：本计划实现的是已改写的契约（`DEC-OPI-ecb3037d-…19` rev 6），其中 GOAL / CORE_JOURNEY / IN_SCOPE / TECHNICAL_CONSTRAINTS 均为 PROPOSE——**实现完成不等于契约已被用户确认**。
- 断言强度纪律：任何"这个教法有效"的说法都不要写进测试名——测试只钉**机制**（泄漏/超档/评分词/换挡），钉不了教学质量。

---

## File Structure

**新增（教学引擎，纯逻辑）**
| 文件 | 职责 |
|---|---|
| `web/units/teach/session.mjs` | 会话拍子状态机（四拍 + 收尾），纯逻辑 |
| `web/units/teach/focus.mjs` | 从学习者内容里挑出**一个**教学焦点 |
| `web/units/teach/method.mjs` | 方法选择 + 防审讯（连续同法即换挡）+ 卡住时降级 |
| `web/units/teach/difficulty.mjs` | 四档难度 + 每档行为约束参数（含回复长度上限） |
| `web/units/teach/prompt.mjs` | 组装六槽位提示词 |
| `web/units/teach/validate.mjs` | **输出机械校验**（焦点词泄漏 / 长度超档 / 评分词）——本形态唯一可机读的护城河 |
| `web/units/teach/knowledge.mjs` | 知识资产装载 + 格式校验 |
| `web/units/teach/profile.mjs` | 学习者画象持久化（方法使用记录是防审讯的前提） |
| `web/units/teach/loop.mjs` | 回合编排：组装 → 调用 → 校验 → 重生成 → 降级 |

**新增（数据 · 内容资产）**
| 文件 | 职责 |
|---|---|
| `web/data/words.json` | 重点词库种子（词 + 释义 + 搭配 + 易混词） |
| `web/data/grammar.json` | 语法点/中式英语模式种子 |
| `web/data/scenes.json` | 情境库种子（角色扮演搭台用） |

**新增（UI）**
| 文件 | 职责 |
|---|---|
| `web/dialogue.mjs` | 对话视图（单线程滚动 + 输入 + 动作按钮），`mountDialogue(root, deps)` |
| `web/dialogue.html` | 新形态的入口页（与旧 `index.html` 并存，**部署时决定哪个是首页**） |
| `web/gallery-dialogue.html` | 新形态的界面走查台（注入假模型，不花钱） |

**保留不动**
`web/app.mjs`、`web/index.html`、`web/units/{recognize,frame-qc,camera,album,pick-word,scheduler,compose,feedback,speak,deepseek,keyring,store,event-log,rounds,pending,state-machine}.mjs` 及其全部测试（543 条基线必须保持全绿——它们测的是旧形态，作为历史依据存在）。

---

### Task 1: 会话拍子状态机

**Files:**
- Create: `web/units/teach/session.mjs`
- Test: `tests/teach-session.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `PHASES: readonly string[]`、`createSession({ onEnter?, now? })` → `{ readonly phase, can(action), send(action, payload?), snapshot() }`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-session.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { PHASES, createSession } from '../web/units/teach/session.mjs';

test('四个拍子 + 收尾，顺序即正常流程', () => {
  assert.deepEqual(PHASES, ['idle', 'intake', 'diagnose', 'stage', 'elicit', 'assist', 'close']);
});

test('构造时以 idle 回调一次（否则首屏没有渲染时机）', () => {
  const seen = [];
  createSession({ onEnter: (p) => seen.push(p) });
  assert.deepEqual(seen, ['idle']);
});

test('onEnter 不是函数时响亮抛错（契约违约，不是用户操作）', () => {
  assert.throws(() => createSession({ onEnter: null }), TypeError);
});

test('正典路径：学习者给内容 → 定焦点 → 搭台 → 逼产出 → 给帮助 → 收尾', () => {
  const m = createSession({ onEnter: () => {} });
  assert.equal(m.send('content'), true);   // idle → intake
  assert.equal(m.phase, 'intake');
  assert.equal(m.send('focusPicked'), true);
  assert.equal(m.phase, 'diagnose');
  assert.equal(m.send('staged'), true);
  assert.equal(m.send('produced'), true);
  assert.equal(m.send('assisted'), true);
  assert.equal(m.send('closed'), true);
  assert.equal(m.phase, 'close');
});

test('**不许跳过逼产出**：elicit 只能由 produced 或 assisted 离开，产出的唯一起点是 stage', () => {
  const m = createSession({ onEnter: () => {} });
  m.send('content'); m.send('focusPicked'); m.send('staged');
  // 从 stage 直接想去 close 是不合法的
  assert.equal(m.can('closed'), false);
  assert.equal(m.send('closed'), false);
  assert.equal(m.phase, 'stage', '非法动作必须静默忽略且不改状态');
});

test('没人愿意说时不许硬逼：elicit 上卡住只能降级到 assist（换方法/给最小帮助）', () => {
  const m = createSession({ onEnter: () => {} });
  m.send('content'); m.send('focusPicked'); m.send('staged');
  assert.equal(m.send('stuck'), true);
  assert.equal(m.phase, 'assist');
});

test('学习者主动放弃：任何活动拍子都能 cancel 回 idle', () => {
  for (const setup of [[], ['content'], ['content', 'focusPicked']]) {
    const m = createSession({ onEnter: () => {} });
    setup.forEach((a) => m.send(a));
    assert.equal(m.send('cancel'), true, `在 ${m.phase} 上应当能取消`);
    assert.equal(m.phase, 'idle');
  }
});

test('快照带拍子停留时长，且形状稳定（每拍都有键）', () => {
  let t = 1000;
  const m = createSession({ onEnter: () => {}, now: () => t });
  t = 1500;
  m.send('content');
  const s = m.snapshot();
  assert.deepEqual(Object.keys(s.dwellMs).sort(), [...PHASES].sort());
  assert.equal(s.dwellMs.idle, 500);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-session.test.mjs`
Expected: FAIL —— `Cannot find module '.../web/units/teach/session.mjs'`

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/session.mjs
//
// 教学会话的拍子状态机：idle → intake → diagnose → stage → elicit → assist → close。
//
// **纯逻辑：零 import、零浏览器 API、零副作用**，可在 Node 中直接测。
//
// 为什么需要它：设计稿 §2.2 的四拍（诊断 → 搭台 → 逼产出 → 给够用的帮助）是**顺序约束**，
// 不是建议。没有状态机，"系统一次把答案讲完"就永远只是提示词里的一句恳求——
// 模型会本能地帮忙帮到底。把它变成转移表之后，"跳步"在结构上不可能。
//
// 两条与 `units/state-machine.mjs` 一致的约定：
//   1. 非法动作**静默忽略**并返回 false（用户点了当前不可用的按钮不是异常）；
//   2. 契约违约（onEnter/now 不是函数）**响亮抛错**。
//
// 代价（如实记）：`close` 之后没有回到 `intake` 的转移——一次会话就是一次会话。
// 想继续练要新开一次（`createSession`），这是有意的：会话边界是"这一轮教了什么"的账本边界。

/** 拍子顺序（快照的 dwellMs 按这个顺序建键，形状稳定）。 */
export const PHASES = Object.freeze([
  'idle', 'intake', 'diagnose', 'stage', 'elicit', 'assist', 'close',
]);

/**
 * 转移表：TRANSITIONS[当前拍子][动作] = 下一拍子。
 *
 * `cancel` 是**每个活动拍子**都有的出口：学习者随时可以不想聊了。它与"卡住"（stuck）
 * 严格分开——stuck 是"想说但说不出来"（要继续教，降到 assist），cancel 是"不想聊了"
 * （结束，什么都不记）。把两者混为一谈会把"放弃"当成"需要帮助"，是最粗暴的一种误诊。
 */
export const TRANSITIONS = Object.freeze({
  idle: Object.freeze({ content: 'intake', cancel: 'idle' }),
  intake: Object.freeze({ focusPicked: 'diagnose', cancel: 'idle' }),
  diagnose: Object.freeze({ staged: 'stage', cancel: 'idle' }),
  stage: Object.freeze({ produced: 'elicit', stuck: 'assist', cancel: 'idle' }),
  // elicit 上的 produced **指回自己**：说出来了就继续说（一次会话可以练好几轮），
  // 直到学习者自己说够了（close）。这与"不许跳过逼产出"不冲突——它只保证
  // **进不来** elicit 以外的入口，不限制在 elicit 里待多久。
  elicit: Object.freeze({ assisted: 'assist', produced: 'elicit', close: 'close', cancel: 'idle' }),
  assist: Object.freeze({ produced: 'close', cancel: 'idle' }),
  close: Object.freeze({}),
});

/** 造一台教学会话状态机。 */
export function createSession({ onEnter, now = Date.now } = {}) {
  if (typeof onEnter !== 'function') {
    throw new TypeError('createSession: onEnter 必须是函数（进入每个拍子时回调，用于渲染）');
  }
  if (typeof now !== 'function') {
    throw new TypeError('createSession: now 必须是返回时间戳的函数');
  }

  let phase = 'idle';
  let enteredAt = now();
  const dwellMs = Object.fromEntries(PHASES.map((p) => [p, 0]));

  const settle = () => {
    dwellMs[phase] += now() - enteredAt;
    enteredAt = now();
  };

  onEnter(phase);

  return {
    get phase() { return phase; },
    can(action) { return Boolean(TRANSITIONS[phase]?.[action]); },
    send(action) {
      const next = TRANSITIONS[phase]?.[action];
      if (!next) return false;           // 非法动作：静默忽略，绝不悄悄改状态
      settle();
      phase = next;
      onEnter(phase);
      return true;
    },
    snapshot() { return { phase, dwellMs: { ...dwellMs } }; },
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-session.test.mjs`
Expected: PASS（8 个用例全绿）

> 若 `elicit` 那条断言失败并报 `phase === 'elic'`，说明你把 `produced` 的目标写成了别的字符串——
> 它必须**恰好**是 `'elicit'`（指回自己）。这是本任务最容易打错的一处。

- [ ] **Step 5: 跑全量确认没碰坏旧形态**

Run: `node --test`
Expected: `pass 543` + 你新增的 8 条 = `pass 551`，`fail 0`

- [ ] **Step 6: 提交**

```bash
git add web/units/teach/session.mjs tests/teach-session.test.mjs
git commit -m "feat(teach): 会话拍子状态机（四拍 + 收尾，不许跳过逼产出）"
```

---

### Task 2: 焦点挑选

**Files:**
- Create: `web/units/teach/focus.mjs`
- Test: `tests/teach-focus.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `FOCUS_SOURCES: readonly string[]`、`makeFocus({kind, ref, meaning, source})` → `Focus`、`pickFocus({content, candidates, exclude})` → `Focus | null`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-focus.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { FOCUS_SOURCES, makeFocus, pickFocus } from '../web/units/teach/focus.mjs';

test('焦点的来源只有两种：学习者带来的 / 系统给的', () => {
  assert.deepEqual(FOCUS_SOURCES, ['learner', 'system']);
});

test('一次只挑一个焦点（这是"一次只教一个点"的结构保证）', () => {
  const f = pickFocus({
    content: '我今天犹豫了很久要不要请假',
    candidates: [
      { kind: 'word', ref: 'hesitate', meaning: '犹豫' },
      { kind: 'structure', ref: 'whether-clause', meaning: 'whether 引导的宾语从句' },
    ],
  });
  assert.equal(Array.isArray(f), false, 'pickFocus 必须返回单个焦点，不是数组');
  assert.equal(f.ref, 'hesitate');
});

test('学习者自己的内容优先：来源标记为 learner', () => {
  const f = pickFocus({ content: '我今天犹豫了很久', candidates: [{ kind: 'word', ref: 'hesitate', meaning: '犹豫' }] });
  assert.equal(f.source, 'learner');
});

test('没有学习者内容时系统给情境，来源标记为 system', () => {
  const f = pickFocus({ content: '', candidates: [{ kind: 'word', ref: 'hesitate', meaning: '犹豫' }] });
  assert.equal(f.source, 'system');
});

test('不重复教同一个焦点：exclude 里的词被跳过', () => {
  const f = pickFocus({
    content: 'x',
    candidates: [{ kind: 'word', ref: 'hesitate', meaning: '犹豫' }, { kind: 'word', ref: 'waver', meaning: '动摇' }],
    exclude: ['hesitate'],
  });
  assert.equal(f.ref, 'waver');
});

test('候选全被排除时返回 null（宁可重来，不许重复教）', () => {
  assert.equal(pickFocus({ content: 'x', candidates: [{ kind: 'word', ref: 'a', meaning: '' }], exclude: ['a'] }), null);
});

test('没有候选时返回 null，不发明一个焦点', () => {
  assert.equal(pickFocus({ content: '我今天很累', candidates: [] }), null);
});

test('makeFocus 拒绝非法 kind / source（契约违约要响亮）', () => {
  assert.throws(() => makeFocus({ kind: 'poem', ref: 'x', meaning: 'y', source: 'learner' }), TypeError);
  assert.throws(() => makeFocus({ kind: 'word', ref: 'x', meaning: 'y', source: 'teacher' }), TypeError);
});

test('焦点状态从"未接触"开始，且是冻结的', () => {
  const f = makeFocus({ kind: 'word', ref: 'hesitate', meaning: '犹豫', source: 'learner' });
  assert.equal(f.status, 'untouched');
  assert.equal(Object.isFrozen(f), true);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-focus.test.mjs`
Expected: FAIL —— `Cannot find module .../focus.mjs`

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/focus.mjs
//
// 焦点：这一次要教的那**一个**点（词 / 结构 / 表达问题）。
//
// **纯逻辑：零 import、零浏览器 API、零副作用。**
//
// 为什么"一次只挑一个"要写进代码而不是提示词：设计稿 §2.3 的引导纪律里
// "一次只教一个点"若只写在提示词里，模型会顺手把三个问题一起说——那是批改，不是教学。
// 把 pickFocus 定成"返回单个对象"之后，"一次多个焦点"在类型上就不可能。
//
// 为什么"学习者优先"是硬规则（设计稿 §2.1 优先级 1）：如果每次都由系统选词，
// 我们其实是在**替学习者规定他该注意什么**——这与"引导他自己去看、自己想"相悖。
// system 来源只在没有学习者内容时使用（由调用方保证，本模块只如实标记来源）。
//
// 代价（如实记）：本模块**不判断**候选是否真的出现在 content 里——它只做挑选与排除。
// "这个词到底是不是他内容里的重点"需要模型参与，那是 loop 的事，不是纯函数的事。

/** 焦点来源。learner = 从他的内容里挑的；system = 系统给的情境里带来的。 */
export const FOCUS_SOURCES = Object.freeze(['learner', 'system']);

const FOCUS_KINDS = Object.freeze(['word', 'structure', 'expression']);
const FOCUS_STATUS = Object.freeze(['untouched', 'recognized', 'usable']);

/**
 * 造一个焦点（冻结对象）。
 * @throws {TypeError} kind / source 不在枚举里（契约违约）
 */
export function makeFocus({ kind, ref, meaning, source }) {
  if (!FOCUS_KINDS.includes(kind)) {
    throw new TypeError(`makeFocus: kind 必须是 ${FOCUS_KINDS.join(' / ')} 之一，收到 ${JSON.stringify(kind)}`);
  }
  if (!FOCUS_SOURCES.includes(source)) {
    throw new TypeError(`makeFocus: source 必须是 ${FOCUS_SOURCES.join(' / ')} 之一，收到 ${JSON.stringify(source)}`);
  }
  return Object.freeze({ kind, ref: String(ref), meaning: String(meaning ?? ''), source, status: FOCUS_STATUS[0] });
}

/**
 * 挑出这一次要教的那一个焦点。
 *
 * @param {{ content?: string, candidates?: Array<{kind,ref,meaning}>, exclude?: string[] }} input
 * @returns {Readonly<object> | null} 挑不出来就返回 null——**宁可这轮不教，也不许重复教**
 */
export function pickFocus({ content = '', candidates = [], exclude = [] } = {}) {
  const skip = new Set(exclude.map(String));
  const usable = (candidates ?? []).filter((c) => c && c.ref != null && !skip.has(String(c.ref)));
  if (usable.length === 0) return null;
  const chosen = usable[0];
  const source = String(content).trim() === '' ? 'system' : 'learner';
  return makeFocus({ kind: chosen.kind, ref: chosen.ref, meaning: chosen.meaning, source });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-focus.test.mjs`
Expected: PASS（9 个用例）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 560`, `fail 0`

```bash
git add web/units/teach/focus.mjs tests/teach-focus.test.mjs
git commit -m "feat(teach): 焦点挑选（一次只挑一个，学习者内容优先）"
```

---

### Task 3: 方法选择与防审讯

**Files:**
- Create: `web/units/teach/method.mjs`
- Test: `tests/teach-method.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `MVP_METHODS`、`ALL_METHODS`、`METHOD_LABELS: Record<string,string>`、`chooseMethod({history, preferred, exclude})` → `string`、`narrowOnStuck(current)` → `string`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-method.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { MVP_METHODS, ALL_METHODS, METHOD_LABELS, chooseMethod, narrowOnStuck } from '../web/units/teach/method.mjs';

test('MVP 只做四种，且必须是九种里的子集', () => {
  assert.deepEqual(MVP_METHODS, ['recall', 'roleplay', 'guess', 'upgrade']);
  assert.equal(MVP_METHODS.every((m) => ALL_METHODS.includes(m)), true);
  assert.equal(ALL_METHODS.length, 9);
});

test('每种方法都有中文标签（界面上要说人话）', () => {
  for (const m of ALL_METHODS) assert.equal(typeof METHOD_LABELS[m], 'string', `${m} 缺标签`);
  assert.equal(METHOD_LABELS.roleplay, '角色扮演');
});

test('**连续两次同一种方法 = 审讯**：必须换掉', () => {
  const next = chooseMethod({ history: ['roleplay', 'roleplay'] });
  assert.notEqual(next, 'roleplay');
});

test('history 里最近一次用过的方法，本轮不选（至少隔一次）', () => {
  assert.notEqual(chooseMethod({ history: ['guess'] }), 'guess');
});

test('全部用过时仍然给一个（轮转回最早用过的那个，而不是返回 null）', () => {
  const next = chooseMethod({ history: [...MVP_METHODS] });
  assert.equal(MVP_METHODS.includes(next), true);
});

test('preferred 被尊重——但连续两次的硬规则优先于偏好', () => {
  assert.equal(chooseMethod({ history: ['guess'], preferred: 'roleplay' }), 'roleplay');
  assert.notEqual(chooseMethod({ history: ['roleplay', 'roleplay'], preferred: 'roleplay' }), 'roleplay');
});

test('卡住时降级到更结构化的方法，且不留在原方法上', () => {
  assert.equal(narrowOnStuck('roleplay'), 'guess');
  assert.equal(narrowOnStuck('recall'), 'guess');
  assert.equal(narrowOnStuck('guess'), 'recall');
  assert.equal(narrowOnStuck('upgrade'), 'recall');
});

test('narrowOnStuck 收到不认识的方法时响亮抛错', () => {
  assert.throws(() => narrowOnStuck('lecture'), TypeError);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-method.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/method.mjs
//
// 提问方法库与换挡（设计稿 §3.2 / §3.4）。
//
// **纯逻辑：零 import、零浏览器 API、零副作用。**
//
// 为什么"方法"是产品的核心资产而不是外壳：要学习者用上 hesitate，问"你今天怎么样"是错的
// ——正确做法是**造一个他必须犹豫的情境**（角色扮演）。同一个词用不同方法教，效果不同，
// 所以需要"方法 × 目标"的匹配；没有一种方法永远好，所以需要换挡。
//
// **防审讯是硬规则**：设计稿 §2.3 写明"任何提问方式连续使用即失效"。用户自己也说过
// "每天都问今天怎么样，会问烦"。所以"连续两次同一种方法"由代码拒绝，不靠提示词自觉。
//
// 代价（如实记）：本模块不认识教学效果。它只能保证"不重复、不连续"，保证不了
// "这次选的方法对这个焦点真的合适"——后者需要真实使用观察（HUMAN_EVALUATION）。

/** 首批四种方法（设计稿 §3.4）：并集刚好覆盖四拍。 */
export const MVP_METHODS = Object.freeze(['recall', 'roleplay', 'guess', 'upgrade']);

/** 全部九种（其余五种第二批，见设计稿 §3.4）。 */
export const ALL_METHODS = Object.freeze([
  'roleplay', 'task', 'describe', 'translate', 'sentence-finish',
  'guess', 'recall', 'reflect', 'upgrade',
]);

export const METHOD_LABELS = Object.freeze({
  roleplay: '角色扮演',
  task: '任务驱动',
  describe: '描述与命名',
  translate: '翻译对照',
  'sentence-finish': '半句接龙',
  guess: '情境猜词',
  recall: '回忆叙述',
  reflect: '纠错反思',
  upgrade: '扩展与升格',
});

/** 卡住时往哪个更结构化的方法降（设计稿 §3.6.2 的降级路径）。 */
const STUCK_FALLBACK = Object.freeze({
  roleplay: 'guess',
  recall: 'guess',
  guess: 'recall',
  upgrade: 'recall',
});

/**
 * 选下一个方法。
 *
 * 规则优先级（写死，不许改顺序）：
 *   1. **连续两次相同 = 拒绝**（防审讯，硬规则，压过偏好）
 *   2. 最近一次用过的也不选（至少隔一次）
 *   3. 尊重 preferred（学习者/引擎的偏好）
 *   4. 否则按 MVP 顺序取第一个没用过的
 *   5. 全用过 → 轮转回**最早**用过的那个（永不返回 null：总得有个方法）
 *
 * @param {{ history?: string[], preferred?: string|null, exclude?: string[] }} input
 * @returns {string}
 */
export function chooseMethod({ history = [], preferred = null, exclude = [] } = {}) {
  const last = history[history.length - 1] ?? null;
  const prev = history[history.length - 2] ?? null;
  const banned = new Set(exclude.map(String));
  // 规则 1：连续两次相同 → 那个方法本轮禁用
  if (last !== null && last === prev) banned.add(last);
  // 规则 2：最近一次用过的不选
  if (last !== null) banned.add(last);

  const usable = MVP_METHODS.filter((m) => !banned.has(m));
  if (preferred && usable.includes(preferred)) return preferred;   // 规则 3
  if (usable.length > 0) return usable[0];                          // 规则 4

  // 规则 5：全用过 → 最早用过的那个（history 里第一次出现的）
  const earliest = MVP_METHODS.find((m) => history.includes(m));
  return earliest ?? MVP_METHODS[0];
}

/**
 * 卡住时的降级方法（换方法，而不是继续逼——设计稿 §3.4.2 行为约束 3）。
 * @throws {TypeError} 不认识的方法名
 */
export function narrowOnStuck(current) {
  const next = STUCK_FALLBACK[current];
  if (!next) {
    throw new TypeError(`narrowOnStuck: 不认识的方法 ${JSON.stringify(current)}；已知：${Object.keys(STUCK_FALLBACK).join(' / ')}`);
  }
  return next;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-method.test.mjs`
Expected: PASS（8 个用例）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 568`, `fail 0`

```bash
git add web/units/teach/method.mjs tests/teach-method.test.mjs
git commit -m "feat(teach): 方法选择与防审讯（连续同法即换挡）"
```

---

### Task 4: 难度四档

**Files:**
- Create: `web/units/teach/difficulty.mjs`
- Test: `tests/teach-difficulty.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `BANDS: readonly number[]`、`BAND_LABELS`、`bandRules(band)` → `{maxChars, allowFirstLetterHint, allowSampleSentence, minHelpSteps}`、`stepUp(band)` / `stepDown(band)` → `number`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-difficulty.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { BANDS, BAND_LABELS, bandRules, stepUp, stepDown } from '../web/units/teach/difficulty.mjs';

test('四档，不引入 CEFR 术语', () => {
  assert.deepEqual(BANDS, [1, 2, 3, 4]);
  assert.deepEqual(BANDS.map((b) => BAND_LABELS[b]), ['入门', '基础', '进阶', '熟练']);
});

test('每档都有长度上限，且随档位放松（回复必须与学习者水平匹配）', () => {
  const caps = BANDS.map((b) => bandRules(b).maxChars);
  assert.deepEqual(caps, [...caps].sort((a, b) => a - b), '高档位的上限不许比低档位更紧');
  assert.equal(new Set(caps).size, 4, '四档的上限必须互不相同，否则分档没有意义');
});

test('入门档允许更直接的帮助（首字母 / 示例句），熟练档不许', () => {
  assert.equal(bandRules(1).allowFirstLetterHint, true);
  assert.equal(bandRules(1).allowSampleSentence, true);
  assert.equal(bandRules(4).allowFirstLetterHint, false);
  assert.equal(bandRules(4).allowSampleSentence, false);
});

test('升档到顶就停住，不越界', () => {
  assert.equal(stepUp(1), 2);
  assert.equal(stepUp(4), 4, '已经在最高档时升档是幂等的，不许变成 5');
});

test('降到低就停住，不越界', () => {
  assert.equal(stepDown(4), 3);
  assert.equal(stepDown(1), 1);
});

test('非法档位响亮抛错（契约违约）', () => {
  assert.throws(() => bandRules(0), TypeError);
  assert.throws(() => bandRules(5), TypeError);
  assert.throws(() => stepUp(2.5), TypeError);
});

test('bandRules 返回冻结对象（不许被下游就地改写）', () => {
  assert.equal(Object.isFrozen(bandRules(2)), true);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-difficulty.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/difficulty.mjs
//
// 难度四档（设计稿 §3.5）。**纯逻辑：零 import、零浏览器 API、零副作用。**
//
// 为什么需要"静态起点"：§3.3 的换挡是**动态**调节（根据信号换方法），但同一个"犹豫"，
// 入门和熟练该用完全不同教法。缺了静态档位，系统会对所有人都用同一种难度。
//
// 三条设计选择（都是有意的，不是默认值）：
//   1. **不引入 CEFR 术语**（A1/B2…）：学习者不需要知道自己是"B1"，那是考试语汇。
//   2. **宁可定低一档**：低一档只是稍微简单，高一档直接挫败——**挫败的代价远大于无聊**。
//      所以 stepDown 比 stepUp 更容易被触发（由 loop 决定，本模块只提供动作）。
//   3. **用行为表现难度，不用数字**：升档表现为"台子变难、帮助变少"，界面上**不显示进度条**。
//      这与 NON_GOALS"不制造分数焦虑"一致。
//
// 代价（如实记）：`maxChars` 是**字数上限**，不是 token 数——中英混排下它只是个粗略尺子。
// 它够用是因为它的职责是"拦住模型的长篇大论"，而不是精确控制长度。

export const BANDS = Object.freeze([1, 2, 3, 4]);

export const BAND_LABELS = Object.freeze({ 1: '入门', 2: '基础', 3: '进阶', 4: '熟练' });

/** 每档的行为参数。上限单位是字符（中英混排的粗略尺子，见文件头"代价"）。 */
const RULES = Object.freeze({
  1: Object.freeze({ maxChars: 60, allowFirstLetterHint: true, allowSampleSentence: true, minHelpSteps: 3 }),
  2: Object.freeze({ maxChars: 120, allowFirstLetterHint: true, allowSampleSentence: false, minHelpSteps: 2 }),
  3: Object.freeze({ maxChars: 200, allowFirstLetterHint: false, allowSampleSentence: false, minHelpSteps: 2 }),
  4: Object.freeze({ maxChars: 320, allowFirstLetterHint: false, allowSampleSentence: false, minHelpSteps: 1 }),
});

function assertBand(band) {
  if (!Number.isInteger(band) || !BANDS.includes(band)) {
    throw new TypeError(`难度档必须是 ${BANDS.join(' / ')} 之一，收到 ${JSON.stringify(band)}`);
  }
}

/** 取某档的行为参数（冻结对象）。@throws {TypeError} 非法档位 */
export function bandRules(band) {
  assertBand(band);
  return RULES[band];
}

/** 升一档（到顶幂等）。@throws {TypeError} 非法档位 */
export function stepUp(band) {
  assertBand(band);
  return BANDS[Math.min(BANDS.indexOf(band) + 1, BANDS.length - 1)];
}

/** 降一档（到底幂等）。@throws {TypeError} 非法档位 */
export function stepDown(band) {
  assertBand(band);
  return BANDS[Math.max(BANDS.indexOf(band) - 1, 0)];
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-difficulty.test.mjs`
Expected: PASS（7 个用例）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 575`, `fail 0`

```bash
git add web/units/teach/difficulty.mjs tests/teach-difficulty.test.mjs
git commit -m "feat(teach): 难度四档（静态起点 + 每档行为参数）"
```

---

### Task 5: 提示词组装（六槽位）

**Files:**
- Create: `web/units/teach/prompt.mjs`
- Test: `tests/teach-prompt.test.mjs`

**Interfaces:**
- Consumes: `Focus`（Task 2）、`bandRules`（Task 4）、`METHOD_LABELS`（Task 3）
- Produces: `SLOTS: readonly string[]`、`assemblePrompt({phase, method, band, focus, learnerState, scene})` → `string`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-prompt.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { SLOTS, assemblePrompt } from '../web/units/teach/prompt.mjs';
import { makeFocus } from '../web/units/teach/focus.mjs';

const focus = makeFocus({ kind: 'word', ref: 'hesitate', meaning: '犹豫', source: 'learner' });

test('六个槽位，一个都不能少（设计稿 §3.6.1）', () => {
  assert.deepEqual(SLOTS, ['phase', 'method', 'band', 'focus', 'learnerState', 'scene']);
});

test('组装出的提示词包含全部六槽的实际内容', () => {
  const p = assemblePrompt({ phase: 'elicit', method: 'roleplay', band: 1, focus, learnerState: 'untouched', scene: '退租时跟房东说要再考虑两天' });
  assert.match(p, /elicit/);
  assert.match(p, /角色扮演/);
  assert.match(p, /入门/);
  assert.match(p, /hesitate/);
  assert.match(p, /untouched/);
  assert.match(p, /退租/);
});

test('缺任一槽位就响亮抛错——**不许发一次"裸聊"调用**', () => {
  const base = { phase: 'elicit', method: 'roleplay', band: 1, focus, learnerState: 'untouched', scene: 'x' };
  for (const slot of SLOTS) {
    const broken = { ...base };
    delete broken[slot];
    assert.throws(() => assemblePrompt(broken), TypeError, `缺 ${slot} 时应当抛错`);
  }
});

test('行为约束写进提示词：不许说出焦点词（逼产出拍最难的一条）', () => {
  const p = assemblePrompt({ phase: 'elicit', method: 'roleplay', band: 2, focus, learnerState: 'recognized', scene: 'x' });
  assert.match(p, /不要说出 hesitate/, '焦点词必须被明确禁止出现在回复里');
  assert.match(p, /不评价|不许评价/, '必须禁止评价学习者的英语');
});

test('长度上限随档位写进提示词（与 difficulty 同源，不另抄一份）', () => {
  const p1 = assemblePrompt({ phase: 'elicit', method: 'guess', band: 1, focus, learnerState: 'untouched', scene: 'x' });
  const p4 = assemblePrompt({ phase: 'elicit', method: 'guess', band: 4, focus, learnerState: 'untouched', scene: 'x' });
  assert.match(p1, /60 字符/);
  assert.match(p4, /320 字符/);
});

test('入门档允许给首字母提示；熟练档的提示词里不许出现"首字母"', () => {
  const p1 = assemblePrompt({ phase: 'assist', method: 'guess', band: 1, focus, learnerState: 'untouched', scene: 'x' });
  const p4 = assemblePrompt({ phase: 'assist', method: 'guess', band: 4, focus, learnerState: 'untouched', scene: 'x' });
  assert.match(p1, /首字母/);
  assert.equal(/首字母/.test(p4), false);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-prompt.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/prompt.mjs
//
// 提示词组装：把六槽位与行为约束拼成这一次要给模型的东西（设计稿 §3.6）。
//
// **纯逻辑：零浏览器 API、零副作用**（只 import 同目录的纯模块）。
//
// 这是整个产品的**护城河所在**：产品不是"接了个模型"，而是"用一套框架约束模型"。
// 所以约束不是角色设定（"你是英语老师"保证不了任何事），而是**行为约束**：
// 不许说出焦点词、不许评价、单次回复长度上限、卡住先降档不先给答案。
//
// 为什么缺槽位要**响亮抛错**而不是补默认值：六槽任一缺失，模型就退化成通用闲聊
// ——那是"裸聊"，不是教学。宁可当次不发请求，也不要发一次没有教学目标的调用。
//
// 代价（如实记）：本模块只**保证约束被写进提示词**，保证不了模型遵守。
// 遵守由 `validate.mjs` 的机械校验兜底——两层是分开的，缺一层都不成立。

import { bandRules } from './difficulty.mjs';
import { METHOD_LABELS } from './method.mjs';

/** 六槽位（固定，不可省）。 */
export const SLOTS = Object.freeze(['phase', 'method', 'band', 'focus', 'learnerState', 'scene']);

const SCORING_BAN = '不得打分、不得说"错了/不正确"、不得比较学习者与他人、不得给出等级或分数。';

/**
 * 组装这一次的提示词。
 * @throws {TypeError} 任一槽位缺失或类型不对（**不许发裸聊调用**）
 */
export function assemblePrompt({ phase, method, band, focus, learnerState, scene } = {}) {
  const given = { phase, method, band, focus, learnerState, scene };
  for (const slot of SLOTS) {
    const v = given[slot];
    if (v === undefined || v === null || v === '') {
      throw new TypeError(`assemblePrompt: 缺少槽位 ${slot}——六槽缺一不发调用（缺了就退化成通用闲聊）`);
    }
  }
  if (!METHOD_LABELS[method]) {
    throw new TypeError(`assemblePrompt: 不认识的方法 ${JSON.stringify(method)}`);
  }

  const rules = bandRules(band);   // 非法档位在这里抛 TypeError，与 difficulty 同一个权威
  const allowHint = rules.allowFirstLetterHint
    ? `他卡住时可以给首字母提示（最多 ${rules.minHelpSteps} 步帮助，之后必须换方法）`
    : '不许给首字母或拼写提示——他自己能想出来';
  const allowSample = rules.allowSampleSentence
    ? '必要时可以给一句示例（但只限一句，且不许替他写出他这段内容的成稿）'
    : '不许给示例句';

  return [
    '【角色】你是这个应用里的英语学习引导者。你的职责不是讲解，是**让他自己说出来**。',
    `【拍子】${phase}（诊断 → 搭台 → 逼产出 → 给够用的帮助 → 收尾）`,
    `【方法】${METHOD_LABELS[method]}（${method}）`,
    `【难度档】${band}（${focus ? '' : ''}${rules.maxChars} 字符上限：你这一次的回复**不得长于 ${rules.maxChars} 字符**）`,
    `【焦点】${focus.ref}（${focus.kind}，含义：${focus.meaning}，来源：${focus.source}）`,
    `【学习者状态】${learnerState}（untouched 未接触 / recognized 认得 / usable 能用）`,
    `【情境】${scene}`,
    '',
    '【必须】为焦点留出使用位置；把决定权留给他（给选项，不替他决定下一步）。',
    `【禁止】不要说出 ${focus.ref} 这个词；不评价他的英语；一次只教这一个点；不连续追问超过 2 轮；不替他写成稿。${SCORING_BAN}`,
    `【帮助尺度】${allowHint}；${allowSample}。`,
    '【卡住时】先降一档难度，仍不行再换方法——**不要直接把答案给他**。',
  ].join('\n');
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-prompt.test.mjs`
Expected: PASS（6 个用例）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 581`, `fail 0`

```bash
git add web/units/teach/prompt.mjs tests/teach-prompt.test.mjs
git commit -m "feat(teach): 六槽位提示词组装（缺一不发调用）"
```

---

### Task 6: 输出机械校验（本形态唯一的机读防线）

**Files:**
- Create: `web/units/teach/validate.mjs`
- Test: `tests/teach-validate.test.mjs`

**Interfaces:**
- Consumes: `bandRules`（Task 4）
- Produces: `checkReply({text, focusRef, band})` → `{ok: boolean, violations: string[]}`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-validate.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { checkReply } from '../web/units/teach/validate.mjs';

const ok = (r) => r.ok === true;
const bad = (r) => r.ok === false;

test('正常回复通过', () => {
  const r = checkReply({ text: 'Are you sure? Take your time.', focusRef: 'hesitate', band: 2 });
  assert.equal(ok(r), true);
  assert.deepEqual(r.violations, []);
});

test('**泄漏焦点词**——逼产出拍最难的一条，必须抓到', () => {
  const r = checkReply({ text: 'You can say "hesitate" here.', focusRef: 'hesitate', band: 2 });
  assert.equal(bad(r), true);
  assert.deepEqual(r.violations, ['focus_leak']);
});

test('泄漏检查不分大小写（模型会把词句首大写）', () => {
  assert.equal(bad(checkReply({ text: 'Hesitate is the word.', focusRef: 'hesitate', band: 2 })), true);
});

test('泄漏检查不误伤"包含该词的长词"（避免假阳性）', () => {
  // hesitated / hesitating 是同一个词族，**也算泄漏**（他要的是自己想起来，不是看到词干）
  assert.equal(bad(checkReply({ text: 'He hesitated.', focusRef: 'hesitate', band: 2 })), true);
  // 但无关词不许误伤
  assert.equal(ok(checkReply({ text: 'Take your time.', focusRef: 'hesitate', band: 2 })), true);
});

test('超长回复被拒（长度上限随档位，与 difficulty 同源）', () => {
  const long = 'a'.repeat(200);
  assert.equal(bad(checkReply({ text: long, focusRef: 'mug', band: 1 })), true);
  assert.equal(ok(checkReply({ text: long, focusRef: 'mug', band: 4 })), true);
});

test('**评分词/等级词**一律拒（NON_GOALS：不制造分数焦虑）', () => {
  for (const t of ['你这句话是错的。', 'Score: 8/10', 'Your level is B2.', '又错了，再想想。', '排名第 3']) {
    const r = checkReply({ text: t, focusRef: 'mug', band: 3 });
    assert.equal(bad(r), true, `应当拒绝：${t}`);
    assert.equal(r.violations.includes('scoring_language'), true, `${t} 应报 scoring_language`);
  }
});

test('多条违规一起报（不是只报第一条）', () => {
  const r = checkReply({ text: `hesitate 是错的，${'x'.repeat(200)}`, focusRef: 'hesitate', band: 1 });
  assert.deepEqual([...r.violations].sort(), ['focus_leak', 'over_length', 'scoring_language']);
});

test('空文本 / 非法入参响亮抛错', () => {
  assert.throws(() => checkReply({ text: '', focusRef: 'mug', band: 1 }), TypeError);
  assert.throws(() => checkReply({ text: 'x', focusRef: '', band: 1 }), TypeError);
  assert.throws(() => checkReply({ text: 'x', focusRef: 'mug', band: 9 }), TypeError);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-validate.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/validate.mjs
//
// 模型输出的**机械校验**（设计稿 §3.6.3 的第二层防护）。
//
// **纯逻辑：零浏览器 API、零副作用。**
//
// 为什么这一层这么重要：这是把"提示词工程"在本项目里**第一次变成可验证工程**的地方。
// 提示词约束天然会被模型违反——模型会本能地"帮忙帮到底"、顺手把那个词说出来、
// 顺手评价一句"这句有点问题"。而这三样恰好都可以**机械检出**，因此可以写断言、可以回归。
// 没有这一层，"框架约束"就只是一句祈祷。
//
// 三条检查（对应契约 TECHNICAL_CONSTRAINTS 第一条）：
//   1. focus_leak        回复里出现了本次焦点（词族也算）
//   2. over_length       回复超过当前难度档的字符上限
//   3. scoring_language  出现评分/等级/比较学习者的词汇
//
// 代价（如实记）：
//   · 词族匹配用**子串**，会有假阳性（焦点词是另一个词的词干时）。宁可假阳性——
//     假阳性的代价是重生成一次，假阴性的代价是**把答案直接给了学习者**，后者更坏。
//   · 评分词表是**保守的黑名单**，抓不到"换个说法的贬低"。它能挡住的是最常见的那几类。

import { bandRules } from './difficulty.mjs';

/** 评分 / 等级 / 比较类词汇（中英各一组）。命中即拒。 */
export const SCORING_PATTERNS = Object.freeze([
  /评分|得分|打分|分数|成绩|等级|级别|排名|第\s*\d+\s*名/,
  /\b\d+\s*\/\s*(?:10|100)\b/,
  /\b(?:score|grade|level|rank|points?)\b/i,
  /\bB\d\b|\bA[12]\b|\bC[12]\b/,          // CEFR 等级不该出现在给学习者的回复里
  /错了|不对|不正确|错误|又错/,
  /\b(?:wrong|incorrect|mistake|error)\b/i,
]);

function assertNonEmptyString(v, name) {
  if (typeof v !== 'string' || v.trim() === '') {
    throw new TypeError(`checkReply: ${name} 必须是非空字符串，收到 ${JSON.stringify(v)}`);
  }
}

/**
 * 校验一次模型回复。
 * @param {{ text: string, focusRef: string, band: number }} input
 * @returns {{ ok: boolean, violations: string[] }} violations 按固定顺序、可含多条
 * @throws {TypeError} text / focusRef 空，或 band 非法
 */
export function checkReply({ text, focusRef, band } = {}) {
  assertNonEmptyString(text, 'text');
  assertNonEmptyString(focusRef, 'focusRef');
  const rules = bandRules(band);   // 非法档位在这里抛 TypeError

  const violations = [];
  const lower = text.toLowerCase();
  const needle = focusRef.toLowerCase();

  // 1. 焦点泄漏（子串匹配 = 词族也算，见文件头"代价"）
  if (needle.length > 0 && lower.includes(needle)) violations.push('focus_leak');

  // 2. 长度
  if ([...text].length > rules.maxChars) violations.push('over_length');

  // 3. 评分语言
  if (SCORING_PATTERNS.some((re) => re.test(text))) violations.push('scoring_language');

  return { ok: violations.length === 0, violations };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-validate.test.mjs`
Expected: PASS（8 个用例）

> 若 `'又错了，再想想。'` 这一条没被 `scoring_language` 抓到，检查 `SCORING_PATTERNS` 里 `错了|不对|不正确|错误|又错` 那一行是否被正确转义。

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 589`, `fail 0`

```bash
git add web/units/teach/validate.mjs tests/teach-validate.test.mjs
git commit -m "feat(teach): 输出机械校验（焦点泄漏 / 超长 / 评分词）"
```

---

### Task 7: 知识资产格式 + 内容种子

**Files:**
- Create: `web/units/teach/knowledge.mjs`、`web/data/words.json`、`web/data/grammar.json`、`web/data/scenes.json`
- Test: `tests/teach-knowledge.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `validateWords(list)` / `validateGrammar(list)` / `validateScenes(list)` → `string[]`（错误清单，空 = 通过）、`findWord(list, headword)` → `entry | null`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-knowledge.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { validateWords, validateGrammar, validateScenes, findWord } from '../web/units/teach/knowledge.mjs';

const load = (n) => JSON.parse(readFileSync(new URL(`../web/data/${n}`, import.meta.url), 'utf8'));

test('词库种子通过格式校验', () => {
  assert.deepEqual(validateWords(load('words.json')), []);
});
test('语法库种子通过格式校验', () => {
  assert.deepEqual(validateGrammar(load('grammar.json')), []);
});
test('情境库种子通过格式校验', () => {
  assert.deepEqual(validateScenes(load('scenes.json')), []);
});

test('词库种子至少 20 条（少于这个数引擎跑不起来）', () => {
  assert.ok(load('words.json').length >= 20, `词库只有 ${load('words.json').length} 条`);
});

test('词条必须有释义与搭配（只有词没有用法 = 教不了）', () => {
  assert.deepEqual(validateWords([{ headword: 'mug' }]).length > 0, true);
  assert.deepEqual(validateWords([{ headword: 'mug', meaning: '马克杯', collocations: ['a coffee mug'] }]).length, 0);
});

test('重复词条被拒（同一个词教两遍会互相打架）', () => {
  const dup = [
    { headword: 'mug', meaning: 'a', collocations: ['x'] },
    { headword: 'mug', meaning: 'b', collocations: ['y'] },
  ];
  assert.deepEqual(validateWords(dup), ['duplicate headword: mug']);
});

test('findWord 按词头查找，找不到返回 null（不发明词条）', () => {
  const list = load('words.json');
  assert.equal(findWord(list, 'mug')?.meaning != null, true);
  assert.equal(findWord(list, 'zzzznotaword'), null);
});

test('validate* 收到非数组时响亮抛错', () => {
  assert.throws(() => validateWords(null), TypeError);
  assert.throws(() => validateGrammar({}), TypeError);
  assert.throws(() => validateScenes('x'), TypeError);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-knowledge.test.mjs`
Expected: FAIL —— 模块与数据文件都不存在

- [ ] **Step 3: 写实现与内容种子**

```js
// web/units/teach/knowledge.mjs
//
// 知识资产的**格式校验**与查找。**纯逻辑：零浏览器 API、零副作用**（不读文件——
// 数据由调用方 import/载入后传进来，这样 Node 与浏览器两条路径同源）。
//
// 为什么内容资产需要校验器（设计稿 §11 风险 7）：**英语教学质量是产品的命**，
// 而这些资产既不是代码也不是测试——如果词条缺释义、搭配写错、同一个词录两遍，
// 引擎再好也在教错的东西。校验器不能保证内容**对**，但能挡住"格式不全"与"重复"这两类
// 机械可检的坏法，让内容审校的人只需要看语义。
//
// 代价（如实记）：格式校验**不等于教学质量背书**。谁审语义、按什么标准验收，
// 目前**没有流程**（已登记为契约载体的 ASSUMPTIONS 假设 4）——这是本形态最容易被低估的风险。

function assertArray(v, name) {
  if (!Array.isArray(v)) {
    throw new TypeError(`${name}: 必须是数组，收到 ${JSON.stringify(v)}`);
  }
}

/** 校验词库，返回错误清单（空数组 = 通过）。 */
export function validateWords(list) {
  assertArray(list, 'validateWords');
  const errors = [];
  const seen = new Set();
  list.forEach((w, i) => {
    if (!w || typeof w.headword !== 'string' || w.headword.trim() === '') errors.push(`[${i}] 缺 headword`);
    if (typeof w?.meaning !== 'string' || w.meaning.trim() === '') errors.push(`[${i}] 缺 meaning`);
    if (!Array.isArray(w?.collocations) || w.collocations.length === 0) errors.push(`[${i}] 缺 collocations`);
    if (w?.headword) {
      const key = w.headword.toLowerCase();
      if (seen.has(key)) errors.push(`duplicate headword: ${key}`);
      seen.add(key);
    }
  });
  return errors;
}

/** 校验语法库。 */
export function validateGrammar(list) {
  assertArray(list, 'validateGrammar');
  const errors = [];
  const seen = new Set();
  list.forEach((g, i) => {
    if (typeof g?.point !== 'string' || g.point.trim() === '') errors.push(`[${i}] 缺 point`);
    if (typeof g?.chineseTrap !== 'string' || g.chineseTrap.trim() === '') errors.push(`[${i}] 缺 chineseTrap`);
    if (typeof g?.fix !== 'string' || g.fix.trim() === '') errors.push(`[${i}] 缺 fix`);
    if (g?.point) {
      if (seen.has(g.point)) errors.push(`duplicate point: ${g.point}`);
      seen.add(g.point);
    }
  });
  return errors;
}

/** 校验情境库。 */
export function validateScenes(list) {
  assertArray(list, 'validateScenes');
  const errors = [];
  const seen = new Set();
  list.forEach((s, i) => {
    if (typeof s?.id !== 'string' || s.id.trim() === '') errors.push(`[${i}] 缺 id`);
    if (typeof s?.setup !== 'string' || s.setup.trim() === '') errors.push(`[${i}] 缺 setup`);
    if (!Array.isArray(s?.fits) || s.fits.length === 0) errors.push(`[${i}] 缺 fits`);
    if (s?.id) {
      if (seen.has(s.id)) errors.push(`duplicate id: ${s.id}`);
      seen.add(s.id);
    }
  });
  return errors;
}

/** 按词头查找（大小写不敏感）。找不到返回 null——**不发明词条**。 */
export function findWord(list, headword) {
  assertArray(list, 'findWord');
  const key = String(headword).toLowerCase();
  return list.find((w) => String(w?.headword).toLowerCase() === key) ?? null;
}
```

`web/data/words.json`（**种子，20 条**；每条的 `meaning` 用英文释义 + 中文点睛，`collocations` 给真实搭配，`confusables` 给易混词——这三样正是"英语专精知识库"里"知道哪些是重点词汇"的最小可用形态）：

```json
[
  { "headword": "hesitate", "meaning": "to pause before doing something because you are unsure（犹豫）", "collocations": ["hesitate to do sth", "hesitate about doing sth"], "confusables": ["waver", "vacillate"] },
  { "headword": "mug", "meaning": "a large cup with a handle, used for coffee or tea（马克杯）", "collocations": ["a coffee mug", "a mug of tea"], "confusables": ["cup", "glass"] },
  { "headword": "kettle", "meaning": "a container used for boiling water（烧水壶）", "collocations": ["put the kettle on", "the kettle is boiling"], "confusables": ["pot", "teapot"] },
  { "headword": "umbrella", "meaning": "a thing you hold over your head when it rains（雨伞）", "collocations": ["take an umbrella", "put up an umbrella"], "confusables": ["parasol"] },
  { "headword": "commute", "meaning": "to travel regularly between home and work（通勤）", "collocations": ["commute to work", "a long commute"], "confusables": ["travel", "transfer"] },
  { "headword": "leftover", "meaning": "food remaining after a meal（剩饭剩菜）", "collocations": ["leftovers for lunch", "eat the leftovers"], "confusables": ["remainder", "residue"] },
  { "headword": "queue", "meaning": "a line of people waiting（排队）", "collocations": ["join the queue", "queue up"], "confusables": ["line (AmE)", "row"] },
  { "headword": "deadline", "meaning": "the latest time by which something must be done（截止时间）", "collocations": ["meet a deadline", "miss the deadline"], "confusables": ["due date", "time limit"] },
  { "headword": "colleague", "meaning": "a person you work with（同事）", "collocations": ["a close colleague", "my colleagues at work"], "confusables": ["coworker", "classmate"] },
  { "headword": "overtime", "meaning": "extra hours worked beyond normal hours（加班）", "collocations": ["work overtime", "do overtime"], "confusables": ["extra hours"] },
  { "headword": "landlord", "meaning": "the owner who rents a place to you（房东）", "collocations": ["pay the landlord", "the landlord raised the rent"], "confusables": ["tenant", "host"] },
  { "headword": "deposit", "meaning": "money paid as a first part or as security（押金/定金）", "collocations": ["pay a deposit", "get the deposit back"], "confusables": ["down payment", "advance"] },
  { "headword": "receipt", "meaning": "a paper proving you paid（收据）", "collocations": ["keep the receipt", "ask for a receipt"], "confusables": ["invoice", "bill"] },
  { "headword": "refund", "meaning": "money returned to you（退款）", "collocations": ["ask for a refund", "a full refund"], "confusables": ["rebate", "return"] },
  { "headword": "appointment", "meaning": "an arranged meeting at a fixed time（预约）", "collocations": ["make an appointment", "cancel an appointment"], "confusables": ["reservation", "booking"] },
  { "headword": "exhausted", "meaning": "extremely tired（精疲力尽）", "collocations": ["feel exhausted", "absolutely exhausted"], "confusables": ["tired", "worn out"] },
  { "headword": "annoyed", "meaning": "slightly angry（有点恼火）", "collocations": ["annoyed with sb", "annoyed about sth"], "confusables": ["angry", "irritated"] },
  { "headword": "grateful", "meaning": "feeling thankful（感激）", "collocations": ["grateful for sth", "grateful to sb"], "confusables": ["thankful", "appreciative"] },
  { "headword": "awkward", "meaning": "embarrassing or difficult to deal with（尴尬/不好处理）", "collocations": ["an awkward silence", "an awkward situation"], "confusables": ["embarrassing", "clumsy"] },
  { "headword": "figure out", "meaning": "to understand or solve something（想明白/弄清楚）", "collocations": ["figure out how to do sth", "figure it out"], "confusables": ["find out", "work out"] }
]
```

`web/data/grammar.json`（中式英语高频模式种子，8 条）：

```json
[
  { "point": "very + 动词", "chineseTrap": "I very like it.", "fix": "I like it very much. / I really like it." },
  { "point": "冠词缺失", "chineseTrap": "I went to bank.", "fix": "I went to the bank." },
  { "point": "过去时与现在时混用", "chineseTrap": "Yesterday I go to the gym and I meet my friend.", "fix": "Yesterday I went to the gym and met my friend." },
  { "point": "there have", "chineseTrap": "There have many people.", "fix": "There are many people." },
  { "point": "open/close 误用", "chineseTrap": "Please open the light.", "fix": "Please turn on the light." },
  { "point": "虽然…但是… 直译", "chineseTrap": "Although it rained, but we went out.", "fix": "Although it rained, we went out." },
  { "point": "borrow/lend 方向反", "chineseTrap": "I borrowed him my book.", "fix": "I lent him my book." },
  { "point": "marry with", "chineseTrap": "She married with him.", "fix": "She married him." }
]
```

`web/data/scenes.json`（角色扮演/任务搭台用，8 条）：

```json
[
  { "id": "rent-talk", "setup": "你要跟房东说要再考虑两天再决定续不续租。", "fits": ["hesitate", "deposit", "landlord"] },
  { "id": "late-work", "setup": "同事问你昨天为什么那么晚才走。", "fits": ["overtime", "deadline", "exhausted"] },
  { "id": "morning-rush", "setup": "早上赶时间，你在跟家人说今天怎么出门。", "fits": ["commute", "queue", "umbrella"] },
  { "id": "cafe-order", "setup": "你在咖啡店点单，想说清楚你要哪一种杯子。", "fits": ["mug", "receipt", "refund"] },
  { "id": "kitchen-chat", "setup": "室友在厨房问你水开了没有、剩饭怎么办。", "fits": ["kettle", "leftover", "mug"] },
  { "id": "apology", "setup": "你迟到了，要跟朋友解释并道歉。", "fits": ["awkward", "grateful", "annoyed"] },
  { "id": "clinic-visit", "setup": "你身体不舒服，在诊所预约并描述症状。", "fits": ["appointment", "exhausted", "figure out"] },
  { "id": "shopping-return", "setup": "你买错了东西，想退货并要回钱。", "fits": ["refund", "receipt", "awkward"] }
]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-knowledge.test.mjs`
Expected: PASS（8 个用例）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 597`, `fail 0`

```bash
git add web/units/teach/knowledge.mjs web/data tests/teach-knowledge.test.mjs
git commit -m "feat(teach): 知识资产格式校验 + 词库/语法库/情境库种子"
```

---

### Task 8: 学习者画象持久化

**Files:**
- Create: `web/units/teach/profile.mjs`
- Test: `tests/teach-profile.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `createProfile({ storage })` → `{ read(), recordMethod(m), recordFocus(ref), recordError(pattern), setBand(b), methodsUsed(), errorPatterns(), band() }`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-profile.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { createProfile, PROFILE_KEY } from '../web/units/teach/profile.mjs';

const fakeStorage = () => {
  const m = new Map();
  return { getItem: (k) => (m.has(k) ? m.get(k) : null), setItem: (k, v) => m.set(k, String(v)), removeItem: (k) => m.delete(k) };
};

test('存储键固定，不与旧形态的键冲突', () => {
  assert.equal(PROFILE_KEY, 'elp.teach.profile');
  assert.notEqual(PROFILE_KEY, 'elp.apiKey');
  assert.notEqual(PROFILE_KEY, 'elp.words');
});

test('空存储上读出来是一份干净画象（不抛错、不发明历史）', () => {
  const p = createProfile({ storage: fakeStorage() });
  assert.deepEqual(p.methodsUsed(), []);
  assert.deepEqual(p.errorPatterns(), []);
  assert.equal(p.band(), 2, '没有依据时宁可定低一档（挫败代价大于无聊）');
});

test('方法使用记录被持久化——这是"防审讯"的前提（不记住就会重复问）', () => {
  const storage = fakeStorage();
  createProfile({ storage }).recordMethod('roleplay');
  const again = createProfile({ storage });
  assert.deepEqual(again.methodsUsed(), ['roleplay']);
});

test('方法记录**只保留最近 N 次**（无限增长等于没记录）', () => {
  const p = createProfile({ storage: fakeStorage() });
  for (const m of ['roleplay', 'guess', 'recall', 'upgrade', 'roleplay', 'guess', 'recall']) p.recordMethod(m);
  const used = p.methodsUsed();
  assert.ok(used.length <= 6, `最近记录应当被截断，实测 ${used.length}`);
  assert.equal(used[used.length - 1], 'recall', '最新的在最后');
});

test('难度档可读可写，写非法档位响亮抛错', () => {
  const p = createProfile({ storage: fakeStorage() });
  p.setBand(3);
  assert.equal(p.band(), 3);
  assert.throws(() => p.setBand(9), TypeError);
});

test('反复出错的结构被记录（去重，且保留最近 N 条）', () => {
  const p = createProfile({ storage: fakeStorage() });
  p.recordError('very-verb');
  p.recordError('very-verb');
  p.recordError('article-missing');
  assert.deepEqual(p.errorPatterns(), ['very-verb', 'article-missing'], '同一条不许记两次');
});

test('存储里的数据坏掉时退回干净画象，而不是把坏数据传下去', () => {
  const storage = fakeStorage();
  storage.setItem(PROFILE_KEY, '{ this is not json');
  const p = createProfile({ storage });
  assert.deepEqual(p.methodsUsed(), []);
  assert.equal(p.band(), 2);
});

test('缺 storage 时响亮抛错（避免静默不持久化——那会让防审讯失效）', () => {
  assert.throws(() => createProfile({}), TypeError);
  assert.throws(() => createProfile({ storage: {} }), TypeError);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-profile.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/profile.mjs
//
// 学习者画象：跨会话记住"用过哪些方法、反复错在哪、当前难度档"。
//
// **纯逻辑：零浏览器 API**（storage 由调用方注入，与 `units/keyring.mjs` / `units/store.mjs` 同一种做法，
// 便于在 Node 里用假存储测）。
//
// 为什么**必须**持久化方法使用记录（设计稿 §10 的关键约束）：设计稿 §2.3 的"不审讯"由
// `method.mjs` 的"连续两次同法即换挡"实现，而"连续两次"需要**跨会话**的历史——
// 会话内记住没用，下次打开又从零开始，用户就会再次被同样的方式问一遍。
// 所以防审讯的有效性**依赖这份记录**。
//
// 代价（如实记）：
//   · 只存最近 6 次方法。更长的历史对"防审讯"没有增量价值，而无限增长会让存储成为负担。
//   · 数据坏掉时**静默退回干净画象**（不抛错）：一份坏掉的画象不该让整个应用打不开，
//     代价是"画象静默重置"——这与 `store.mjs` 对坏数据的处理口径一致。
//   · 仍然没有账号：这份画象只在本机。清浏览器数据即清空（已登记为契约 COST/PRIVACY 风险 3）。

/** 存储键（与旧形态的 elp.apiKey / elp.words / elp.events 并列，互不覆盖）。 */
export const PROFILE_KEY = 'elp.teach.profile';

const MAX_METHOD_HISTORY = 6;
const MAX_ERROR_PATTERNS = 8;
const DEFAULT_BAND = 2;   // 宁可定低一档，见 difficulty.mjs 文件头

function assertStorage(storage) {
  if (!storage || typeof storage.getItem !== 'function' || typeof storage.setItem !== 'function') {
    throw new TypeError('createProfile: storage 必须提供 getItem/setItem（缺了会让防审讯静默失效）');
  }
}

/** 造一份学习者画象（读写都走注入的 storage）。 */
export function createProfile({ storage } = {}) {
  assertStorage(storage);

  const readRaw = () => {
    try {
      const raw = storage.getItem(PROFILE_KEY);
      if (raw === null || raw === undefined || raw === '') return { methods: [], errors: [], band: DEFAULT_BAND };
      const parsed = JSON.parse(raw);
      return {
        methods: Array.isArray(parsed?.methods) ? parsed.methods.filter((m) => typeof m === 'string') : [],
        errors: Array.isArray(parsed?.errors) ? parsed.errors.filter((e) => typeof e === 'string') : [],
        band: Number.isInteger(parsed?.band) ? parsed.band : DEFAULT_BAND,
      };
    } catch {
      return { methods: [], errors: [], band: DEFAULT_BAND };   // 坏数据静默退回干净画象
    }
  };

  const write = (data) => {
    try {
      storage.setItem(PROFILE_KEY, JSON.stringify(data));
    } catch {
      // 存储满了：不抛错（不该让学习流程因为写不进去而中断），代价是这一次没记住
    }
  };

  return {
    methodsUsed() { return readRaw().methods; },
    errorPatterns() { return readRaw().errors; },
    band() { return readRaw().band; },
    setBand(band) {
      if (!Number.isInteger(band) || band < 1 || band > 4) {
        throw new TypeError(`setBand: 难度档必须是 1..4 的整数，收到 ${JSON.stringify(band)}`);
      }
      write({ ...readRaw(), band });
    },
    recordMethod(method) {
      const d = readRaw();
      const methods = [...d.methods, String(method)].slice(-MAX_METHOD_HISTORY);
      write({ ...d, methods });
    },
    recordError(pattern) {
      const d = readRaw();
      const key = String(pattern);
      if (d.errors.includes(key)) return;
      write({ ...d, errors: [...d.errors, key].slice(-MAX_ERROR_PATTERNS) });
    },
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-profile.test.mjs`
Expected: PASS（9 个用例）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 606`, `fail 0`

```bash
git add web/units/teach/profile.mjs tests/teach-profile.test.mjs
git commit -m "feat(teach): 学习者画象持久化（方法记录是防审讯的前提）"
```

---

### Task 9: 回合编排（组装 → 调用 → 校验 → 重生成 → 降级）

**Files:**
- Create: `web/units/teach/loop.mjs`
- Test: `tests/teach-loop.test.mjs`

**Interfaces:**
- Consumes: `assemblePrompt`（T5）、`checkReply`（T6）、`chooseMethod` / `narrowOnStuck`（T3）、`bandRules` / `stepDown`（T4）
- Produces: `runTurn({ generate, ...input })` → `{ text, method, band, degraded: boolean, attempts: number }`

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-loop.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { runTurn, MAX_ATTEMPTS } from '../web/units/teach/loop.mjs';
import { makeFocus } from '../web/units/teach/focus.mjs';

const focus = makeFocus({ kind: 'word', ref: 'hesitate', meaning: '犹豫', source: 'learner' });
const base = { phase: 'elicit', method: 'roleplay', band: 2, focus, learnerState: 'untouched', scene: '退租' };

test('模型给出合规回复：直接采用，不重试', async () => {
  const r = await runTurn({ ...base, generate: async () => 'Are you sure? Take your time.' });
  assert.equal(r.text, 'Are you sure? Take your time.');
  assert.equal(r.degraded, false);
  assert.equal(r.attempts, 1);
});

test('**泄漏焦点词 → 重生成**（这是这一层存在的理由）', async () => {
  let n = 0;
  const r = await runTurn({
    ...base,
    generate: async () => { n += 1; return n === 1 ? 'Say hesitate here.' : 'Are you sure?'; },
  });
  assert.equal(r.text, 'Are you sure?');
  assert.equal(r.attempts, 2);
  assert.equal(r.degraded, false);
});

test('重生成仍违规 → **降级为模板话术**（保证下限，不把违规内容给学习者）', async () => {
  const r = await runTurn({ ...base, generate: async () => 'hesitate hesitate hesitate' });
  assert.equal(r.degraded, true);
  assert.equal(r.attempts, MAX_ATTEMPTS);
  assert.equal(/hesitate/i.test(r.text), false, '降级话术里绝不许出现焦点词');
});

test('降级话术长度符合当前难度档（降级也要守约束）', async () => {
  const r = await runTurn({ ...base, band: 1, generate: async () => 'hesitate' });
  assert.ok([...r.text].length <= 60, `降级话术超长：${[...r.text].length}`);
});

test('模型抛错 → 也算一次失败，最终降级（不许把异常吞掉后返回空字符串）', async () => {
  const r = await runTurn({ ...base, generate: async () => { throw new Error('network'); } });
  assert.equal(r.degraded, true);
  assert.ok(r.text.length > 0, '降级话术必须非空');
});

test('每次尝试都用新提示词（把违规原因回灌给模型，否则它会重复犯同一个错）', async () => {
  const prompts = [];
  await runTurn({ ...base, generate: async ({ prompt }) => { prompts.push(prompt); return 'hesitate'; } });
  assert.equal(prompts.length, MAX_ATTEMPTS);
  assert.notEqual(prompts[0], prompts[1], '第二次的提示词必须与第一次不同（带上了上次违规的原因）');
});

test('generate 不是函数时响亮抛错', async () => {
  await assert.rejects(() => runTurn({ ...base, generate: null }), TypeError);
});

test('maxChars 收紧发生在重试上：第二次尝试会降一档难度', async () => {
  const bands = [];
  await runTurn({
    ...base, band: 3,
    generate: async ({ prompt }) => { bands.push(/难度档】(\d)/.exec(prompt)?.[1]); return 'hesitate'; },
  });
  assert.deepEqual(bands, ['3', '2'], '第一次违规后应当降档重试（卡住先降档，不先给答案）');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-loop.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/loop.mjs
//
// 一个回合的编排：组装提示词 → 调模型 → 机械校验 → 重生成 → 降级为模板话术。
//
// **纯逻辑**：不碰浏览器 API、不自己发请求——`generate` 由调用方注入
// （浏览器路径注入真 DeepSeek 调用，测试注入假实现）。这样这一层能在 Node 里确定性地测。
//
// 为什么必须有"降级"这一档（设计稿 §3.6.3 第三层）：提示词约束一定会被违反。
// 如果只是"重试一次然后放弃"，那么在最坏情况下我们**要么把违规内容给学习者**
// （泄漏了焦点词 = 把答案给了），要么给一片空白。两者都不行。所以最后一档是
// **模板话术**——它不含任何教学判断，因此不可能违规，保证了下限。
//
// 为什么重试时**降一档**而不是原样重试：设计稿 §3.4.2 的行为约束 3 写着
// "卡住时先降档，不先给答案"。模型第一次没能在不泄漏的前提下把台子搭起来，
// 通常意味着台子对这个学习者偏难——降档比重复同一个请求更可能成功。
//
// 代价（如实记）：`MAX_ATTEMPTS = 2` 意味着**最坏情况一次回合发 2 次请求**，
// 这直接进成本（契约 COST_CONSTRAINTS 那条"单次会话调用上限"）。调大它会线性抬高花费。

import { assemblePrompt } from './prompt.mjs';
import { checkReply } from './validate.mjs';
import { bandRules, stepDown } from './difficulty.mjs';

/** 每个回合最多尝试几次（含第一次）。再失败就走模板话术。 */
export const MAX_ATTEMPTS = 2;

/**
 * 降级用的模板话术。**不含任何教学判断**，因此不可能违规。
 * 它仍然完成"留出使用位置"这件事：给学习者一个说话的机会。
 */
function fallbackText({ method, band }) {
  const cap = Math.min(bandRules(band).maxChars, 40);
  const byMethod = {
    roleplay: '你先说，我听着。',
    recall: '说说看，今天有什么想讲的？',
    guess: '你先猜一个，不着急。',
    upgrade: '换个说法再讲一遍？',
  };
  const text = byMethod[method] ?? '你先说说看。';
  return [...text].slice(0, cap).join('');
}

/**
 * 跑一个回合。
 *
 * @param {{
 *   generate: (input: { prompt: string, attempt: number }) => Promise<string>,
 *   phase: string, method: string, band: number, focus: object, learnerState: string, scene: string
 * }} input
 * @returns {Promise<{ text: string, method: string, band: number, degraded: boolean, attempts: number }>}
 * @throws {TypeError} generate 不是函数
 */
export async function runTurn({ generate, phase, method, band, focus, learnerState, scene } = {}) {
  if (typeof generate !== 'function') {
    throw new TypeError('runTurn: generate 必须是函数（把模型调用注入进来，本模块不自己发请求）');
  }

  let currentBand = band;
  let lastViolations = [];

  for (let attempt = 1; attempt <= MAX_ATTEMPTS; attempt += 1) {
    const prompt = assemblePrompt({
      phase, method, band: currentBand, focus, learnerState, scene,
      // 把上一次的违规原因回灌（第 2 次尝试起）——否则模型会原样再犯
      ...(lastViolations.length > 0 ? { retryHint: lastViolations } : {}),
    });

    let text = '';
    try {
      text = String(await generate({ prompt, attempt }));
    } catch {
      lastViolations = ['generate_failed'];
      currentBand = stepDown(currentBand);
      continue;
    }

    const verdict = checkReply({ text, focusRef: focus.ref, band: currentBand });
    if (verdict.ok) {
      return { text, method, band: currentBand, degraded: false, attempts: attempt };
    }

    lastViolations = verdict.violations;
    currentBand = stepDown(currentBand);   // 卡住先降档，不先给答案
  }

  return { text: fallbackText({ method, band: currentBand }), method, band: currentBand, degraded: true, attempts: MAX_ATTEMPTS };
}
```

> `assemblePrompt` 目前不认 `retryHint`，`assemblePrompt` 会把多余的键忽略掉（它只读六槽）——**但测试要求第二次的提示词与第一次不同**。为此在 Task 5 的 `assemblePrompt` 里补一个可选参数：签名改为 `assemblePrompt({ ..., retryHint = null })`，并在拼装末尾加一段
> `...(retryHint ? ['', `【上次的问题】${retryHint.join(' / ')}——这次务必避开。`] : [])`。
> **这是本计划里唯一的跨任务修改**：它是 Task 9 能成立的前提，改动落回 Task 5 的文件与其测试（给 `tests/teach-prompt.test.mjs` 加一条：传 `retryHint` 时提示词包含它）。

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/teach-loop.test.mjs tests/teach-prompt.test.mjs`
Expected: PASS（loop 8 条 + prompt 7 条）

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 615`, `fail 0`

```bash
git add web/units/teach/loop.mjs web/units/teach/prompt.mjs tests/teach-loop.test.mjs tests/teach-prompt.test.mjs
git commit -m "feat(teach): 回合编排（校验 → 降档重试 → 模板话术兜底）"
```

---

### Task 10: 对话视图

**Files:**
- Create: `web/dialogue.mjs`
- Test: `tests/dialogue-mount.test.mjs`

**Interfaces:**
- Consumes: `createSession`（T1）、`pickFocus`（T2）、`chooseMethod`（T3）、`bandRules`（T4）、`runTurn`（T9）、`createProfile`（T8）
- Produces: `mountDialogue(root, deps)` → `Promise<void>`；`deps = { storage, generate, candidates, scenes }`

- [ ] **Step 1: 写失败的测试**

```js
// tests/dialogue-mount.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { mountDialogue } from '../web/dialogue.mjs';
import { makeEl, byTag, text } from './helpers/dom.mjs';

const fakeStorage = () => {
  const m = new Map();
  return { getItem: (k) => (m.has(k) ? m.get(k) : null), setItem: (k, v) => m.set(k, String(v)), removeItem: (k) => m.delete(k) };
};

const mount = async (over = {}) => {
  const root = makeEl('div');
  await mountDialogue(root, {
    storage: fakeStorage(),
    generate: async () => 'Are you sure? Take your time.',
    candidates: [{ kind: 'word', ref: 'hesitate', meaning: '犹豫' }],
    scenes: [{ id: 'rent-talk', setup: '你要跟房东说要再考虑两天。', fits: ['hesitate'] }],
    ...over,
  });
  return { root };
};

test('首屏要求学习者先说话（不是先给词，也不是先出题）', async () => {
  const { root } = await mount();
  assert.match(text(root), /说|写/, '首屏要邀请他讲自己的事');
});

test('提交内容后进入对话：出现系统的回复', async () => {
  const { root } = await mount();
  const box = byTag(root, 'TEXTAREA')[0];
  box.value = '我今天犹豫了很久要不要请假';
  byTag(root, 'BUTTON').find((b) => b.textContent.includes('发送')).click();
  await new Promise((r) => setTimeout(r, 10));
  assert.match(text(root), /Are you sure/, '系统的回复要出现在对话里');
});

test('系统回复里出现焦点词就是失败（视图层不许绕过校验）', async () => {
  const { root } = await mount({ generate: async () => 'just say hesitate' });
  const box = byTag(root, 'TEXTAREA')[0];
  box.value = '我今天犹豫了很久';
  byTag(root, 'BUTTON').find((b) => b.textContent.includes('发送')).click();
  await new Promise((r) => setTimeout(r, 10));
  assert.equal(/hesitate/i.test(text(root)), false, '视图不许把泄漏焦点词的回复渲染出来');
});

test('界面上不出现打分/等级字样（NON_GOALS：不制造分数焦虑）', async () => {
  const { root } = await mount();
  const shown = text(root);
  for (const bad of ['分数', '等级', '评分', '排名', '第 1 档']) {
    assert.equal(shown.includes(bad), false, `界面上不该出现「${bad}」`);
  }
});

test('缺 generate 时响亮抛错（不许静默挂一个不能说话的对话）', async () => {
  await assert.rejects(() => mount({ generate: null }), TypeError);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/dialogue-mount.test.mjs`
Expected: FAIL —— `Cannot find module .../web/dialogue.mjs`

- [ ] **Step 3: 写最小实现**

```js
// web/dialogue.mjs
//
// 新形态的对话视图：一个单线程的滚动对话 + 一个输入框。
//
// **与旧形态的 `web/app.mjs` 并存，互不 import**（`DEC-…db.56` 把识物降级为
// "主动求助时的一条查询路径"，不删除）。等新形态跑通、用户确认之后，再决定谁做首页。
//
// 视图的职责边界（有意收窄）：它**只做渲染与收集输入**。所有的教学判断
// （挑焦点、选方法、定难度、组装提示词、校验输出）都在 `web/units/teach/**` 的纯模块里。
// 这样"教什么"可以在 Node 里被确定性地测，而"长什么样"才需要浏览器。
//
// 代价（如实记）：MVP 是**单线程对话**，没有历史会话列表、没有作品回看
// （设计稿 §8 的作品化是下一步）。没有它，"拥有感"这一条还没兑现。

import { createSession } from './units/teach/session.mjs';
import { pickFocus } from './units/teach/focus.mjs';
import { chooseMethod } from './units/teach/method.mjs';
import { runTurn } from './units/teach/loop.mjs';
import { createProfile } from './units/teach/profile.mjs';

const EMPTY_PROMPT = '说说你今天干了什么，或者你现在想说什么——中文就行。';

/**
 * 把对话挂到容器上。
 * @param {HTMLElement} root
 * @param {{ storage: object, generate: Function, candidates?: Array, scenes?: Array }} deps
 * @throws {TypeError} generate 不是函数
 */
export async function mountDialogue(root, deps = {}) {
  const { storage, generate, candidates = [], scenes = [] } = deps;
  if (typeof generate !== 'function') {
    throw new TypeError('mountDialogue: generate 必须是函数（模型调用由调用方注入）');
  }
  const doc = root.ownerDocument ?? globalThis.document;
  const profile = createProfile({ storage });

  const transcript = [];
  const log = doc.createElement('div');
  log.className = 'dialogue-log';
  const input = doc.createElement('textarea');
  input.placeholder = EMPTY_PROMPT;
  input.rows = 3;
  const send = doc.createElement('button');
  send.textContent = '发送';
  send.className = 'primary';
  const row = doc.createElement('div');
  row.className = 'row';
  row.append(send);
  root.replaceChildren(log, input, row);

  const push = (who, body) => {
    transcript.push({ who, body });
    const el = doc.createElement('p');
    el.className = who === 'system' ? 'say-system' : 'say-learner';
    el.textContent = body;
    log.append(el);
  };

  const session = createSession({ onEnter: () => {} });
  push('system', EMPTY_PROMPT);

  send.addEventListener('click', async () => {
    const content = String(input.value ?? '').trim();
    if (content === '') return;
    input.value = '';
    push('learner', content);
    session.send('content');
    session.send('focusPicked');

    const focus = pickFocus({ content, candidates, exclude: [] });
    if (focus === null) {
      push('system', '这个我一时接不上——换一件今天的事说说？');
      return;
    }

    const method = chooseMethod({ history: profile.methodsUsed(), preferred: null });
    profile.recordMethod(method);
    const scene = scenes[0]?.setup ?? `围绕「${focus.meaning}」的一个日常情境`;
    session.send('staged');

    const turn = await runTurn({ generate, phase: 'elicit', method, band: profile.band(), focus, learnerState: 'untouched', scene });
    push('system', turn.text);
  });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/dialogue-mount.test.mjs`
Expected: PASS（5 个用例）

> 若 `makeEl` 造的假元素不支持 `ownerDocument`，在测试里改用 `tests/helpers/dom.mjs` 的既有做法——读该文件确认它导出的助手形状（`makeEl` / `byTag` / `text` 已在本仓库使用），必要时补一个 `ownerDocument: { createElement: makeEl }`。

- [ ] **Step 5: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 620`, `fail 0`

```bash
git add web/dialogue.mjs tests/dialogue-mount.test.mjs
git commit -m "feat(teach): 对话视图（单线程 + 输入框，教学判断全在纯模块里）"
```

---

### Task 11: 入口页与走查台

**Files:**
- Create: `web/dialogue.html`、`web/gallery-dialogue.html`
- Modify: `web/styles.css`（追加对话样式，**只用既有令牌**）
- Test: `tests/dialogue-html.test.mjs`

**Interfaces:**
- Consumes: `mountDialogue`（T10）、`units/deepseek.mjs`（已有）、`units/keyring.mjs`（已有）
- Produces: 无导出（页面）

- [ ] **Step 1: 写失败的测试**

```js
// tests/dialogue-html.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';

const html = readFileSync(new URL('../web/dialogue.html', import.meta.url), 'utf8');
const gallery = readFileSync(new URL('../web/gallery-dialogue.html', import.meta.url), 'utf8');
const css = readFileSync(new URL('../web/styles.css', import.meta.url), 'utf8');

test('入口页引用样式单源，且没有内联 <style>（不许回流）', () => {
  assert.match(html, /<link rel="stylesheet" href="\.\/styles\.css">/);
  assert.equal(/<style[\s>]/.test(html), false, '两处样式源会各自漂移');
});

test('入口页零外部资源（本机离线也能开）', () => {
  const ext = [...html.matchAll(/(?:src|href)\s*=\s*"(?:https?:)?\/\//g)];
  assert.deepEqual(ext.map((m) => m[0]), []);
});

test('走查台注入假模型，结构上不可能花钱', () => {
  assert.match(gallery, /generate/, '走查台必须注入假 generate');
  assert.equal(/api\.deepseek\.com/.test(gallery), false, '走查台不许出现真实端点');
});

test('对话样式只用令牌，不写死颜色', () => {
  const body = css.replace(/\/\*[\s\S]*?\*\//g, '');
  const mine = body.slice(body.indexOf('.dialogue-log'));
  assert.ok(mine.length > 0, 'styles.css 里找不到对话样式');
  assert.equal(/#[0-9a-fA-F]{3,8}\b/.test(mine), false, '对话样式里不许出现写死的十六进制颜色');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/dialogue-html.test.mjs`
Expected: FAIL —— 页面不存在

- [ ] **Step 3: 写页面与样式**

`web/dialogue.html`：

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>英语学习 · 说说你的生活</title>
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16'%3E%3Crect width='16' height='16' rx='4' fill='%230f766e'/%3E%3C/svg%3E">
  <link rel="stylesheet" href="./styles.css">
</head>
<body>
  <main id="app"></main>
  <script type="module">
    import { mountDialogue } from './dialogue.mjs';
    import { createKeyring } from './units/keyring.mjs';
    import { DEEPSEEK_API_BASE, DEEPSEEK_MODEL, chatUrl, extractContent } from './units/deepseek.mjs';
    import words from './data/words.json' with { type: 'json' };
    import scenes from './data/scenes.json' with { type: 'json' };

    const keyring = createKeyring({ storage: localStorage });
    const root = document.getElementById('app');

    /** 真的模型调用（只有这一处碰网络）。Key 来自 learner 自己的 localStorage。 */
    const generate = async ({ prompt }) => {
      const key = keyring.read();
      if (!key) throw new Error('NO_KEY');
      const res = await fetch(chatUrl(DEEPSEEK_API_BASE), {
        method: 'POST',
        headers: { 'content-type': 'application/json', authorization: `Bearer ${key}` },
        body: JSON.stringify({
          model: DEEPSEEK_MODEL,
          messages: [{ role: 'user', content: prompt }],
          temperature: 0.7,
        }),
      });
      return extractContent(await res.json());
    };

    const candidates = words.map((w) => ({ kind: 'word', ref: w.headword, meaning: w.meaning }));
    await mountDialogue(root, { storage: localStorage, generate, candidates, scenes });
  </script>
</body>
</html>
```

> ⚠️ `keyring.read()` 的方法名以 `web/units/keyring.mjs` 的实际导出为准（读该文件确认；若是 `get()` 就用 `get()`）。**不要猜**——`tests/keyring.test.mjs` 里有权威形状。

`web/gallery-dialogue.html`：与上面同构，但把 `generate` 换成假实现，且**不 import deepseek**：

```html
<script type="module">
  // 走查台：注入假模型。结构上不可能花钱，也不碰 keyring。
  const FAKE = {
    elicit: 'Are you sure? Take your time.',
    assist: 'You could start with: "I was not sure…"',
  };
  const generate = async ({ prompt }) => (prompt.includes('assist') ? FAKE.assist : FAKE.elicit);
  // …与 dialogue.html 相同的候选/情境装载，storage 用假实现（内存 Map）
</script>
```

`web/styles.css` **追加**（全部走既有令牌；插到文件末尾）：

```css
/* ── 对话视图（新形态）──────────────────────────────────────────────────────
   只用既有令牌：颜色/圆角/间距/时长都不许写死（有测试钉住）。 */
.dialogue-log {
  display: flex;
  flex-direction: column;
  gap: var(--s-2, 12px);
  padding-block: var(--s-3, 16px);
}
.say-system,
.say-learner {
  max-width: 85%;
  margin: 0;
  padding: 10px 14px;
  border-radius: var(--r);
  line-height: 1.6;
  overflow-wrap: anywhere;
}
.say-system {
  align-self: flex-start;
  background: var(--surface);
  border: 1px solid var(--line);
  color: var(--ink);
}
.say-learner {
  align-self: flex-end;
  background: var(--accent-wash);
  border: 1px solid var(--accent);
  color: var(--ink);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/dialogue-html.test.mjs`
Expected: PASS（4 个用例）

- [ ] **Step 5: 眼睛验收（浏览器实际打开一次）**

Run: `node tmp/serve-web.mjs 4189`（后台），然后浏览器开 `http://127.0.0.1:4189/gallery-dialogue.html`
Expected: 看到对话视图；输入一句话点发送，出现系统回复；**页面上不出现"分数/等级/评分"**。
把截图存到 `docs/ui-redesign/after/teach-dialogue-first.png`。

- [ ] **Step 6: 跑全量 + 提交**

Run: `node --test`
Expected: `pass 624`, `fail 0`

```bash
git add web/dialogue.html web/gallery-dialogue.html web/styles.css tests/dialogue-html.test.mjs docs/ui-redesign/after/teach-dialogue-first.png
git commit -m "feat(teach): 新形态入口页 + 走查台 + 对话样式（零外部资源）"
```

---

### Task 12: 作品回看与导出（兑现「拥有感」与隐私承诺）

**Files:**
- Create: `web/units/teach/work.mjs`
- Modify: `web/dialogue.mjs`（追加"回看"与"导出"两个按钮）
- Test: `tests/teach-work.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `appendTurn(list, turn)` → `list'`、`renderWork(list)` → `string[]`、`exportWork(list)` → `string`（JSON）

- [ ] **Step 1: 写失败的测试**

```js
// tests/teach-work.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { appendTurn, renderWork, exportWork, WORK_KEY } from '../web/units/teach/work.mjs';

const t = (d, learner, system) => ({ date: d, learner, system });

test('存储键与旧形态不冲突', () => {
  assert.equal(WORK_KEY, 'elp.teach.work');
});

test('回合按时间追加，顺序不许乱（作品是一本按天翻的日记）', () => {
  let list = [];
  list = appendTurn(list, t('2026-09-17', 'a', 'b'));
  list = appendTurn(list, t('2026-09-18', 'c', 'd'));
  assert.deepEqual(list.map((x) => x.date), ['2026-09-17', '2026-09-18']);
});

test('appendTurn 不改原数组（纯函数，调用方要留旧状态）', () => {
  const list = [];
  const next = appendTurn(list, t('2026-09-17', 'a', 'b'));
  assert.deepEqual(list, [], '原数组必须没被就地改动');
  assert.equal(next.length, 1);
});

test('renderWork 按天分组，给人话（不是原始 JSON）', () => {
  const lines = renderWork([t('2026-09-17', '我今天犹豫了很久', 'Are you sure?')]);
  assert.equal(lines.length, 1);
  assert.match(lines[0], /2026-09-17/);
  assert.match(lines[0], /我今天犹豫了很久/);
});

test('**导出是隐私承诺的兑现**：能导出、且是合法 JSON', () => {
  const out = exportWork([t('2026-09-17', 'a', 'b')]);
  const parsed = JSON.parse(out);
  assert.equal(Array.isArray(parsed), true);
  assert.equal(parsed[0].learner, 'a');
});

test('空作品导出空数组，不抛错（第一次用就有导出按钮）', () => {
  assert.deepEqual(JSON.parse(exportWork([])), []);
});

test('appendTurn 拒绝形状不对的回合（契约违约响亮抛错）', () => {
  assert.throws(() => appendTurn([], null), TypeError);
  assert.throws(() => appendTurn([], { date: '2026-09-17' }), TypeError);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/teach-work.test.mjs`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 写最小实现**

```js
// web/units/teach/work.mjs
//
// 作品：学习者写过的每一天。**纯逻辑：零浏览器 API、零副作用。**
//
// 为什么"作品"是这一形态里最重要的可视化物（设计稿 §8）：契约 IN_SCOPE 承诺了
// 「个人进度与已掌握词可视化」，但新形态里**没有分数可看**（NON_GOALS 禁掉了分数焦虑），
// 于是进度只能靠另一种东西承载——**翻回去看你自己第一天写的东西**。
// 这份东西同时兑现三件事：拥有感、留存来源（留存来自"这是我写的"而非打卡）、复现的素材。
//
// 为什么导出必须在 MVP 里（设计稿 §11 风险 3）：没有账号 = 长期语料只在本机，
// **清一次浏览器数据就清空全部积累**。契约 PRIVACY_CONSTRAINTS 写明"可导出、可删除"，
// 而这是唯一能兑现它的东西。它不是锦上添花的功能，是这个形态的产品级风险处置。
//
// 代价（如实记）：MVP 只做导出，**没做导入**。也就是说换设备能带走数据、但还不能搬回来。
// 导入需要版本与合并策略（两台设备各写了一半怎么办），不在本轮范围。

/** 存储键。 */
export const WORK_KEY = 'elp.teach.work';

function assertTurn(turn) {
  if (!turn || typeof turn !== 'object') {
    throw new TypeError(`回合必须是一个对象，收到 ${JSON.stringify(turn)}`);
  }
  if (typeof turn.date !== 'string' || turn.date.trim() === '') {
    throw new TypeError('回合缺 date（作品要按天分组，没有日期就没法分组）');
  }
  if (typeof turn.learner !== 'string' || typeof turn.system !== 'string') {
    throw new TypeError('回合缺 learner / system 文本');
  }
}

/** 追加一个回合，返回**新数组**（不改原数组）。 */
export function appendTurn(list, turn) {
  if (!Array.isArray(list)) throw new TypeError('appendTurn: list 必须是数组');
  assertTurn(turn);
  return [...list, { date: turn.date, learner: turn.learner, system: turn.system }];
}

/** 渲染成人话（每个回合一行，带日期）。 */
export function renderWork(list) {
  if (!Array.isArray(list)) throw new TypeError('renderWork: list 必须是数组');
  return list.map((t) => `${t.date}　你说：${t.learner}　我回：${t.system}`);
}

/** 导出成 JSON 字符串（隐私承诺的兑现路径）。 */
export function exportWork(list) {
  if (!Array.isArray(list)) throw new TypeError('exportWork: list 必须是数组');
  return JSON.stringify(list, null, 2);
}
```

- [ ] **Step 4: 在 `web/dialogue.mjs` 里接上（追加两个按钮）**

在 `mountDialogue` 的 `root.replaceChildren(...)` 之后追加：

```js
  // 作品回看与导出：契约 IN_SCOPE「学习记录可导出、可删除」的界面出口。
  const workRow = doc.createElement('div');
  workRow.className = 'row';
  const reviewBtn = doc.createElement('button');
  reviewBtn.textContent = '回看我写过的';
  const exportBtn = doc.createElement('button');
  exportBtn.textContent = '导出记录';
  workRow.append(reviewBtn, exportBtn);
  root.append(workRow);

  let work = [];
  reviewBtn.addEventListener('click', () => {
    for (const line of renderWork(work)) push('system', line);
  });
  exportBtn.addEventListener('click', () => {
    const blob = doc.createElement('textarea');
    blob.value = exportWork(work);
    blob.readOnly = true;
    root.append(blob);   // MVP：直接摊出来让学习者自己复制（下载与文件选择器留到后面）
  });
```

并在原有的 `push('system', turn.text)` 之后追加一行：

```js
    work = appendTurn(work, { date: new Date().toISOString().slice(0, 10), learner: content, system: turn.text });
```

补 import：`import { appendTurn, renderWork, exportWork } from './units/teach/work.mjs';`

给 `tests/dialogue-mount.test.mjs` **追加**两条：

```js
test('有"回看我写过的"与"导出记录"两个出口（作品化与隐私承诺的界面出口）', async () => {
  const { root } = await mount();
  const labels = byTag(root, 'BUTTON').map((b) => b.textContent);
  assert.equal(labels.some((t) => t.includes('回看我写过的')), true);
  assert.equal(labels.some((t) => t.includes('导出记录')), true);
});

test('说过一句话之后，回看里能看到它（作品不是空壳）', async () => {
  const { root } = await mount();
  const box = byTag(root, 'TEXTAREA')[0];
  box.value = '我今天犹豫了很久';
  byTag(root, 'BUTTON').find((b) => b.textContent.includes('发送')).click();
  await new Promise((r) => setTimeout(r, 10));
  byTag(root, 'BUTTON').find((b) => b.textContent.includes('回看我写过的')).click();
  assert.match(text(root), /我今天犹豫了很久/, '回看里必须能看到自己写的内容');
});
```

- [ ] **Step 5: 跑测试 + 提交**

Run: `node --test tests/teach-work.test.mjs tests/dialogue-mount.test.mjs`
Expected: PASS（7 + 7 条）

Run: `node --test`
Expected: `pass 633`, `fail 0`

```bash
git add web/units/teach/work.mjs web/dialogue.mjs tests/teach-work.test.mjs tests/dialogue-mount.test.mjs
git commit -m "feat(teach): 作品回看 + 导出（兑现拥有感与隐私承诺）"
```

---

## Self-Review

**1. Spec coverage（逐节对照设计稿）**

| 设计稿章节 | 落在哪个 Task |
|---|---|
| §1 第一原则（相机退居次要） | File Structure 的"保留不动"段（识物代码与其测试全留，不进新路径） |
| §2.1 内容从哪来（学习者优先） | T2 焦点挑选（`source` 标记 + `pickFocus` 顺序） |
| §2.2 四拍循环 | T1 会话状态机 |
| §2.3 引导纪律（四项硬约束） | T1（不跳步）+ T3（不审讯）+ T5/T6（不评价、不制造焦虑） |
| §3.2 方法目录九种 | T3 `ALL_METHODS` |
| §3.3 换挡判据 | T3 `chooseMethod` 的四条规则 + `narrowOnStuck` |
| §3.4 MVP 首批四种 | T3 `MVP_METHODS` |
| §3.5 难度四档 | T4 |
| §3.5.2 宁可定低一档 | T4 `DEFAULT_BAND = 2` + T8 空画象默认档 |
| §3.5.4 不显示进度条 | T10 测试"界面上不出现分数/等级/第 N 档" |
| §3.6 框架约束（六槽位 / 行为约束 / 三层防护） | T5（槽位+约束文本）+ T6（机械校验）+ T9（降级兜底） |
| §3.6.3 三条机械可校验 | T6 三条 violation |
| §4 知识资产六项 | T7（词库/语法库/情境库种子 + 校验器）；学习者语料见 T12 |
| §5 复现改为对话内自然复现 | **未覆盖**（见下方缺口 1） |
| §6 契约冲突清单 | 已在 dmcp 契约重写中落地（非代码任务） |
| §7 资产清算 | File Structure 的"保留不动"段 |
| §8 作品化 | T12 |
| §9 新 GOAL 三条命题 | 命题 3（表达变好）无法在 MVP 内验——需多会话数据（见缺口 2） |
| §11 风险 3（语料丢失） | T12 导出 |
| §11 风险 1（成本） | T9 `MAX_ATTEMPTS = 2` 明确写进代价；**上限与告知未实现**（缺口 3） |
| §11 风险 7（内容正确性） | T7 校验器挡住格式类坏法；**语义背书仍无流程**（缺口 4） |

**2. 缺口清单（诚实登记，不假装覆盖）**

1. **对话内自然复现（§5）未实现**：需要"把旧焦点编进新情境"的机制，依赖跨会话的焦点状态与情境匹配，比 MVP 大一圈。**下一轮第一件该做的事**。
2. **命题 3（表达是否变好）不可验**：需要同一学习者多会话的产出对比。MVP 只保证**能产出与能留存**，不保证**可对比**。
3. **单次会话调用上限与成本告知未实现**：`MAX_ATTEMPTS` 只管住了**每次回合**的上限；**整场会话**的回合数没有上限，也没有"预计花费"的告知。这是契约 `COST_CONSTRAINTS` 的明文要求，**未兑现**。
4. **内容语义审校无流程**：T7 只做格式校验。谁审词条的英文是否正确、按什么标准——仍无答案（契约载体 ASSUMPTIONS 假设 4）。
5. **发音跟读未接**：`FUTURE_SCOPE` 说"保留但不评分"，本轮完全没做（连 TTS 示范都没接）。

**3. Placeholder scan**：全文无 `TBD` / `TODO` / "稍后补充"；每个代码步骤都给了可运行代码。Task 9 的"跨任务修改"（给 `assemblePrompt` 加 `retryHint`）已写明具体改法与测试，不是含糊引用。Task 11 里有两处"以实际导出为准"的提示（`keyring` 方法名、`makeEl` 形状）——这是**故意**的：写计划时无法确认那两个既有助手的精确签名，**猜一个假签名比让人去读一眼更坏**。

**4. Type consistency**：`pickFocus` 返回 `Focus | null`（T2）→ T9/T10 都按 `null` 分支处理；`Focus` 有 `{kind, ref, meaning, source, status}`，T5 读 `ref/kind/meaning/source`，T6 读 `ref`，一致；`bandRules(band)` 在 T4/T5/T6/T9 四处调用，均传整数档位；`checkReply` 返回 `{ok, violations}`，T9 只用这两个键；`runTurn` 返回 `{text, method, band, degraded, attempts}`，T10 只用 `text`。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-09-17-教学引擎-MVP.md`.

**Two execution options:**

1. **Subagent-Driven（推荐）** — 每个 Task 派一个全新上下文的子代理，任务之间由总控复核，迭代快。与 `AGENTS.md` 红线 0（总控不亲自执行）一致。
2. **Inline Execution** — 在本会话里按 `executing-plans` 批量执行，带检查点。

**Which approach?**
