# 本项目无需任何服务端配置（Task 12C 起 server 代理已退役）。
#
# 这个文件只为说明而保留（.gitignore 的 `!.env.example` 规则引用它）：
#   · 模型调用是**浏览器直连** `https://api.deepseek.com`（CORS 已实测放行）；
#   · API Key 由访问者在页面「设置（API Key）」里自己填入，存**本机 localStorage**
#     （键 `elp.apiKey`），不经任何服务器、不入库；
#   · 本地开发不需要任何环境变量：`npx serve web` 或任意静态服务器即可。
#
# 历史注：转向前（server 代理时代）本文件曾承载 DEEPSEEK_API_KEY / PORT /
# PUBLIC_BASE_URL 等服务端配置，那些键随 server/ 一起退役。
